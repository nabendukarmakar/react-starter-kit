import KeyMirror from 'keymirror';

export default {
  VERSION: '0.0.1',
};

export const ActionTypes = KeyMirror({
  SAMPLE_ACTION: null,
});

export const EventTypes = KeyMirror({
  SAMPLE_EVENT: null,
});
